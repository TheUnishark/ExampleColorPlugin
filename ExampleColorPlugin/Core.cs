using BrokeProtocol.API;

namespace ExampleColorPlugin
{
    public class Core : Plugin
    {
        public Core()
        {
            Info = new PluginInfo("ExampleColorPlugin", "ecp", "Example plugin for the Color System");
        }
    }
}
